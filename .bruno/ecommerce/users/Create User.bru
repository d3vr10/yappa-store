meta {
  name: Create User
  type: http
  seq: 1
}

post {
  url: {{SERVER_URL}}/users
  body: json
  auth: inherit
}

body:json {
  {
    "username": "herewego",
    "email": "{{USER_EMAIL_1}}",
    "password": "supergg!",
    "isAdmin": true
  }
}

settings {
  encodeUrl: true
}
