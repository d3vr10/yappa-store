meta {
  name: insertOne
  type: http
  seq: 2
}

post {
  url: {{SERVER_URL}}/categories
  body: json
  auth: inherit
}

body:json {
  {
    "name": "runaway",
    "description": "Those things you use when you desperately need to run away to somewhere where you can live in peace (or anywhere else) for comfort and wellbeing"
  }
}

settings {
  encodeUrl: true
}
